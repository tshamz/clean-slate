{%- capture arguments -%}
  {%- include 'argument',
    _name: 'quantity',
    _required: false,
    _value: quantity,
    _type: '',
    _description: '',
    _default: 1 -%}
{%- endcapture -%}

{%- capture errors -%}
  {%- include 'errors' with arguments, snippet: 'quantity-select' -%}
{%- endcapture -%}

{%- if quantity == blank -%}1{%- endif -%}

{%- if errors == '' -%}
  <div class="oo-quantity-selector {{ classes }}" {{ extra_attrs }}>
    {% include 'button',
      text: '-',
      type: 'button',
      extra_attrs: 'data-quantity-change="-1"' %}
    {% include 'input',
      type: 'number',
      value: quantity,
      extra_attrs: 'min=0',
      name: 'qty' %}
    {% include 'button',
      text: '+',
      type: 'button',
      extra_attrs: 'data-quantity-change="+1"' %}
  </div>
{%- else -%}
  {{ errors }}
{%- endif -%}

{%- assign arguments = blank -%}
{%- assign errors = blank -%}
{%- assign quantity = blank -%}
